name: dome_server
description: A server app using the shelf package and Docker.
version: 1.0.0
# homepage: https://www.example.com

environment:
  sdk: '>=2.18.0-120.0.dev <3.0.0'

dependencies:
  args: ^2.0.0
  json_annotation: ^4.5.0
  mongo_dart: ^0.7.4+1
  shelf: ^1.1.0
  shelf_router: ^1.0.0

dev_dependencies:
  build_runner: ^2.1.11
  http: ^0.13.0
  json_serializable: ^6.2.0
  lints: ^2.0.0
  test: ^1.15.0
